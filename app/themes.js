const color = {
  default: 'green',
};

const fontSize = {
  small: 14,
  default: 16,
  large: 18,
};

const themesStyles = {
  defaultHeader: {
    height: 40,
    backgroundColor: color.default,
    elevation: 0,
    shadowOpacity: 0,
  },
};

const themesText = {
  defaultHeaderTitle: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: fontSize.default,
  },
};


export default {
  color,
  fontSize,
  themesStyles,
  themesText
};

